# mathed_biblio

Bibliography & citation backend for the math editor, wrapping `hayagriva`
(P11.21): cite resolution, numbered cite popups, and the translation layer
for `\cite(...)` markup used by the editor's reveal/expansion machinery.