# typst_element

Typst `Element` wrappers for the math editor's render path: shims over the
Typst layout/element model used by `typst_imaging` and the kanva renderers
to drive markup from Rust without writing Typst source by hand.